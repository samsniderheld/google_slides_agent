model_name: PresentationResponse
description: Schema for presentation responses
fields:
  slides:
    type: list[dict]
    description: List of slide objects
    schema:
      slide_type:
        type: str
        description: Type of slide (e.g., "intro", "content","conclusion")
      slide_content:
        type: list[str]
        description: A list of text strings thats correpond to the input concept and supplied text lengths
